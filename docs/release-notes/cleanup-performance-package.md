# Пакет чистки и оптимизации — пункты 5–6

Отправная точка: `f012deee837f2d37e4252aaa6ef79214adef2e54`.
Рабочая ветка: `codex/cleanup-performance-package`.

## Состав

- Удалены пять модулей без входящих импортов: UI `FormElements`, `MediaCard` и старые Home `AudiobooksGrid`, `PodcastsGrid`, `MixesGrid`. Действующие страницы подкастов/аудиокниг и библиотечные карточки остаются.
- Удалён `date-fns` только из frontend; backend использует его и остаётся без изменений. `@types/howler` относится к devDependencies, сам Howler сохранён.
- Финальный split-frontend образ получает только production-зависимости и не содержит `.next/cache`. Кэш npm остаётся в BuildKit, пользовательские загрузки/кэши не затрагиваются.
- Перемешивание пропускает исчерпанные группы исполнителей: они отсортированы по убыванию размера, поэтому можно завершить внутренний цикл без изменения результата. Это оптимизация CPU подготовки очереди, не скорости получения аудио от YouTube.
- Селекторы тестов Волны учитывают доступные названия кнопок при коротких видимых подписях; сохранены проверки фактического запуска, паузы, настроек и очереди.

## План и приёмка

1. [x] Проверить граф импортов и прямые ссылки перед удалением; сохранить изменения из других рабочих деревьев.
2. [x] Замерить исходный образ и алгоритм; воспроизвести провал новой проверки до исправления.
3. [x] Удалить неиспользуемое и внести точечные оптимизации.
4. [x] Пройти unit/component, lint/types, чистую Docker-сборку и smoke фактического production-образа.
5. [x] Один итоговый adversarial-review — CLEAN; frontend выпущен с сохранением предыдущего образа и overlay для отката.
6. [x] Проверить production снаружи; зафиксировать версию и результаты.

## Проверки

- `npm run test:unit`
- `npm run test:component`
- `npm run lint` (имеющийся warning-бюджет не изменён)
- `npx tsc --noEmit`
- `node --import tsx scripts/benchmark-artist-separation.ts`
- `node scripts/smoke-production-image.mjs IMAGE`

Исходный образ: `node_modules` 612 МиБ; `.next` 149 МиБ, включая 116 МиБ build cache; `date-fns` 28 МиБ. На синтетической очереди 10 000 записей, половина одного исполнителя, исходная обработка около 55 мс, оптимизированная около 1 мс на той же машине. Это не измерение ёмкости production или музыкального холодного старта.

Результаты проверенного образа `local/soundspan-frontend:cleanup-83d16b5e`:

- Git revision: `83d16b5e878526e05dcfc35d0800dd29427829fa`.
- OCI compressed size: 316 002 832 → 182 883 275 байт (−42,1%). Архив для передачи: 182 909 440 байт.
- Runtime: `node_modules` 396 МиБ, `.next` 33 МиБ; суммарно на 332 МиБ меньше этих двух директорий.
- Unit: 1591/1591; component: 1242/1242; Dockerfile checks: 22/22.
- Typecheck: PASS; lint: 0 ошибок, 100 существующих предупреждений; бюджет не повышался.
- Production npm audit: 0 известных уязвимостей; версии зависимостей не обновлялись.
- Чистые npm ci и Next build: PASS. Отдельная установка 59 production-пакетов заняла около 29 секунд; вариант npm prune отклонён из-за длительных операций overlayfs.
- Image smoke: non-root, отсутствие build cache и dev-инструментов, загрузка runtime-модулей, login/library/vibe/runtime-config/service worker, HTTP JS asset + immutable header, структурированный API 503 при недоступном backend, корректное завершение SIGTERM — PASS.
- Performance: повторный одинаковый skewed-сценарий 56,1 → 0,79 мс; ручной benchmark отделён от CI-тестов, чтобы не вводить нестабильный временной порог.

## Adversarial review

Вердикт: CLEAN для данного пакета. Ранний выход обоснован сортировкой групп, идентичность результата проверена; порядок элементов внутри исполнителя и исходный массив сохранены. Удалённые модули не имеют входящих импортов. Runtime-модули и страницы проверены именно в облегчённом образе. Секреты, пользовательские файлы и данные не входят в diff. Отдельный образ и резервный compose overlay обеспечивают откат без миграций.

Ограничения: этот выпуск не меняет алгоритм музыкальных рекомендаций, не гарантирует устранение всех соседних повторов исполнителя, не является нагрузочной приёмкой 100 слушателей и не заменяет физическую проверку iPhone. AIO-образ не переводился на облегчённые зависимости; production использует split-frontend.

## Границы выпуска

Только frontend; без миграций, изменений БД, credentials, источников музыки, серверных загрузок, backup-политики и соседних сервисов. Git push выполняет владелец. Пакет собирается из Git-архива, а не из исторического грязного дерева.

## Выпуск

Production переключён на `local/soundspan-frontend:cleanup-83d16b5e`; состояние healthy получено через 7934 мс после начала переключения. Внешние `/health`, `/api/health`, `/login`, `/library`, `/vibe`, `/runtime-config`, `/sw.js` вернули HTTP 200. Backend, worker, streamer и БД не перезапускались.

Контрольная сумма переданного архива SHA-256: `e3656dd949db30be92d190ea6bc2eded43afd073f8e2b8313c58e3bc0ff567c8`; совпала локально, на Proxmox и в CT121. Передача была медленной; уменьшение размера не выдаётся за подтверждённое ускорение сети/диска.

Для отката сохранены образ `local/soundspan-frontend:cleanup-f012deee` и `/srv/music/soundspan-releases/b0-b340a7c/compose-before-83d16b5e.json`. Восстановить его как `compose.json` и выполнить обычный production compose `up -d --no-deps frontend` с теми же четырьмя base overlay и env-файлом. Проверка и автoоткат находятся в `deploy-frontend-83d16b5e.sh` той же release-директории. Переносимые tar-копии удалены с сервера после загрузки; локальный архив и загруженный образ сохранены.

Последующая корректировка smoke-теста фиксирует существующий API-контракт 503 (не 502) и код `API_PROXY_UNAVAILABLE`. Она и эта запись выпуска не меняют runtime образа с указанной Git revision.
