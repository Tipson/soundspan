# Проверка источников и резервного подбора — 17 сентября 2026

## Выпуск

В production выпущен backend-модуль `musicSources/resolver` из `523ed9fc`.
Образ: `sha256:6668d1f39301df4efa0f5f3f65a60ce28b1915a1ebe034c82fa756a049d2ce62`.
Время: 00:42:03 MSK. Frontend, worker, настройки источников и схема БД не менялись.

Подтверждён и исправлен отказ резерва: зависший поиск или probe первого источника
расходовал все 16 секунд resolver, поэтому доступный второй источник не получал
попытки. Оставшееся время распределяется между доступными источниками. Выбранный
единственный источник получает весь бюджет. Отмена слушателем прекращает подбор;
таймер получения потока не ограничивает уже выданный lease.

## Матрица текущей проверки

| Источник                          | Фактическая проверка                                                                                                         | Результат и границы                                                                                                                    |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| YouTube Music                     | Полный «Альянс — На заре», 6 081 526 байт, 345.381 с; FFmpeg decode; дальний Range и повтор начала совпадают с полным файлом | Пройдено для этой записи. Доступный серверный кэш не доказывает холодное извлечение произвольного видео или отсутствие будущей CAPTCHA |
| VK Music, подключение v7          | «На заре» и «Пьяная»: поиск, lookup, строгий resolver, полные MP3, FFmpeg decode, два дальних диапазона и повтор             | Пройдено; «Пьяная» имеет explicit-метаданные. Для Billie Jean четыре допустимых кандидата: выбор отклонён как неоднозначный            |
| Yandex Music, подключение v2      | «На заре», «Пьяная», Billie Jean: поиск, lookup, resolver, полные MP3, FFmpeg decode, два дальних диапазона и повтор         | Пройдено. «Пьяная» имеет explicit-метаданные; Billie Jean становится резервом после неоднозначного VK                                  |
| Audius                            | Флаг production выключен                                                                                                     | Не считается подключённым источником в этой приёмке                                                                                    |
| Spotify / Deezer                  | Метаданные и импорт в текущей архитектуре                                                                                    | Не выдаются за источники полного воспроизведения                                                                                       |
| Другие провайдеры LavaSrc / TIDAL | Нет активного общего runtime полного playback; TIDAL retired                                                                 | Не входят в список принятых источников                                                                                                 |

## Проверка переключения и идентичности

- verify: исходные два теста stalled search/probe падали; после исправления
  резерв успешно разрешается. Всего семь новых регрессий проверяют также общий
  deadline, отмену на обоих этапах, единичный источник и жизненный цикл lease.
- verify: отдельный контейнер с новой production image и реальными серверными
  подключениями получил аудио Яндекса после искусственного зависания VK search
  за 9.17 с и VK probe за 8.90 с. Быстрый отказ авторизации занял 0.94 с;
  неоднозначное совпадение — 1.31 с. В эти замеры включены контрольные Range.
  При начальном отказе YouTube simulated 503 выбранный lease остаётся тем же
  для последующей перемотки. Production-провайдеры не отключались.
- verify: через публичный HTTPS обычный тестовый пользователь получил полные
  файлы VK и Яндекса; SHA-256 совпал с декодированными файлами. Дальняя перемотка
  вернула 206 с точными байтами. Чужой lease вернул 404, доступ обычного
  пользователя к административной диагностике — 403.
- verify: публичный resolver без принудительного выбора источника выбрал Яндекс
  для Billie Jean после неоднозначного VK.
- verify: полный backend coverage — 614 suites, 8726 tests passed, 9 существующих
  skipped. Backend build и repository gates прошли. Frontend recovery — 17
  поведенческих проверок, включая позицию, отмену и поздний ответ. Это не новый
  физический тест телефона и не реальный обрыв аудио посередине композиции.
- verify: тесты matcher сохраняют отказ от известных clean/preview/live/remix/
  cover-замен, несовпадения исполнителя/длительности и неоднозначности.

Explicit-метка не доказывает, что каждый участок записи не заглушён правообладателем.
Скрытая цензура при неверных или отсутствующих метаданных автоматически не
определяется. Выборка не устанавливает покрытие всего каталога или устойчивую
ёмкость сервисных аккаунтов. Авторизация слушателей во внешних сервисах не нужна.

## Ревью, резерв и завершение

Обычное и adversarial review: CLEAN для изменённой области. Проверены общий
deadline, отмена, закрытие stalled probe, классификация failure/cancellation,
ограничение соединений и неизменность транспортного представления.

Резерв: 23 файла конфигурации и pg_dump 39 437 062 байта, проверенный через
`pg_restore --list`; полный restore в этом выпуске не выполнялся. Release directory:
`/srv/music/soundspan-releases/source-audit-20260917` на CT121. Откат:
`python3 /srv/music/soundspan-releases/source-audit-20260917/release.py rollback`.
Guard проверяет отсутствие изменений операторской конфигурации.

verify: runtime SHA-256 совпал с проверенным артефактом; обе сети backend
сохранены, 14 соседних контейнеров не изменились. Backend healthy, без рестартов
и OOM. Публичный health 200. В коротком проверенном окне после выпуска в журналах
backend/frontend/YouTube не было streamFailure, timeout или fatal. Логи отдельного
pot-provider недоступны из-за logging driver; отсутствие ошибок там не утверждается.

verify: временные admin/user были созданы как test accounts; по завершении их
парольный вход отключён, admin понижен, tokenVersion изменён, прежние токены
проверены на 401 через публичный HTTPS. Секретные файлы проверки удалены,
временный контейнер завершён. История реальных пользователей не изменялась.
Git push не выполнялся.
