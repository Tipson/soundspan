# Плотность действий и жест плеера

Статус: код проверен целевыми тестами; финальная сборка и выпуск ожидаются.

## Пакет

- Панели альбома, исполнителя, YouTube Music, обычного плейлиста и «Любимых треков»: воспроизведение, перемешивание и «Ещё». Подписанные второстепенные действия вынесены в общий Modal через portal, вне stacking context обложки.
- Сохранение, скачивание и применение лайков оставляют список открытым для отображения прогресса. Открытие следующего диалога сначала синхронно закрывает список, чтобы восстановление фокуса не перехватывало новый диалог.
- Верхняя ручка мобильного плеера получает `touch-action: none`; остальной контент сохраняет обычное поведение прокрутки. Ручка очереди уже имела это ограничение.
- Правило плотности закреплено в AGENTS.md. Зоны нажатия не уменьшены.

## Доказательства

- verify: 69 component tests, exit 0, включая проверки всех пяти панелей, состояний плеера, фокуса, прав владельца и прежних сценариев Home/Wave.
- verify: браузерная копия — альбом «Группа крови» на 320/390 px, закрытие списка через Back без ухода со страницы; исполнитель 2CELLOS и его список действий на 1280 px; каталожный альбом на 390 px. Полный визуальный аудит всех экранов не проводился.
- verify: Pixel 10 Pro, Chrome (не standalone), системный свайп `640 480 → 640 1050`, 600 ms: исходная ручка с `touch-action: auto` вызвала reload (timeOrigin 1788820539242.5 → 1788820767863.4). Временное применение точного исправления `none` и повтор того же свайпа закрыли плеер с неизменным timeOrigin 1788820767863.4. Изменение DOM было диагностическим, не выпуском. CDP-синтетический touch сам по себе исходный баг не воспроизводил.
- lint: изменённые панели без ошибок/предупреждений; при добавлении OverlayPlayer строгий `--max-warnings=0` не проходит из-за двух существовавших предупреждений `set-state-in-effect` (восстановление вкладки и состояния drawer). Их присутствие подтверждено в HEAD; текущая правка их не меняет и не подавляет.

## Короткое adversarial review

Scope: панели действий и локальное ограничение жеста. Verdict: CLEAN для проверенного diff; выпуск отдельно зависит от сборки.

Сохранены исходные обработчики и условия доступа к удалению/публичности; дополнительного API и изменений схемы нет. Проверены открытие следующего диалога, восстановление фокуса, Escape и портал поверх hero. Покупки, удаления коллекций и массовые лайки в live-проверке не выполнялись. Проверка собственного скачивания с реальными файлами и проверка iPhone не входят в этот проход.

Дополнительное наблюдение: у каталожной страницы `/album/8243c375-025b-4bef-a825-7400f8fece5e` нет общей кнопки Play при наличии строк треков. Видимость определяется прежней логикой albumActionVisibility; эта правка её не меняет. Отдельно проверить связь каталожных записей с доступностью общего воспроизведения.

## Выпуск и откат

Сборочное дерево: `91b1e02d53b90e1061a94bde5e2bf6f6bb96607f`.
Предыдущий production frontend: `local/soundspan-frontend:android-ba4889cc`.
Кандидат: `local/soundspan-frontend:ui-density-91b1e02d53b90e1061a94bde5e2bf6f6bb96607f`.

Скрипт выпуска меняет только `services.frontend.image`, проверяет исходный образ и source-tree кандидата, сохраняет compose-before и возвращает его при ошибке health/smoke. Backend/worker, данные и пользовательские загрузки не изменяются. Заблокированный выпуск backend Волны не возобновляется этим пакетом.
