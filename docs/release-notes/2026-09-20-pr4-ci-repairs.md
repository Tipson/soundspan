# PR 4 CI repairs — 2026-09-20

## Changes

- Next.js and eslint-config-next 16.3.3; sharp 0.35.4 with updated libvips assets; anyio 4.14.2 in both affected hash-locked Python environments. Backend lock also updates multer to 2.4.0 and js-yaml to 3.15.2 / 4.3.2 following npm audit findings.
- OIDC component harness uses `https://soundspan.test/settings` instead of opaque `about:blank`. The original tests passed on Node 24.14 but all three URL-dependent cases failed on CI's Node 24.20. The five cases pass on 24.20 with the explicit origin; production sign-in logic is unchanged.
- Package-owned Prettier versions format the shared contract and playback test. The shared package uses Prettier 3.9.6; application packages use 3.8.2.
- Gitleaks ignores exactly two historical commit/file/rule/line fingerprints: a synthetic INTERNAL_API_SECRET fixture and Audius documentation prose. The 187-commit PR scan passes; a new synthetic credential in the same test path is still detected. No rules or paths are globally excluded.

## Image-request safety

CodeQL's later completion prompted a DNS-guard review. A local HTTP-server regression reproduced an image request reaching loopback after the compatibility guard's 30-second TTL expired before connection. Active image requests retain a reference-counted guard until response headers arrive or the request rejects. Active entries cannot expire or be evicted; a 256-host capacity fails closed. Each redirect remains manually resolved and revalidated. Finally blocks release the guard; overlapping releases are idempotent. The compatibility cache remains unchanged for other callers.

The new regression fails before the fix and passes after it. Guard, image transport and redirect suites cover 94 cases, including concurrent retention, cache eviction, capacity, private destinations, byte caps, deadline handling and cancellation. Podcast cache mocks retain real outbound-module exports while overriding only DNS prevalidation.

## CodeQL triage and release boundary

Exact alerts 59, 61, 62 and 63 were individually dismissed as false positives with evidence: array membership in a test, a notification deduplication ID, a recommendation experiment bucket and Three.js scene UUIDs. No scanner was disabled and no query/path was excluded.

Image SSRF alerts 57 and 58 remain open pending owner push and analysis of the patched PR. CodeQL may still need evidence-based triage of the custom DNS sanitizer; local tests are not a claim that GitHub's check is green. Unrelated existing alerts were not bulk-dismissed.

Changes are local until the owner pushes. No production deployment, history rewriting or automatic push is included.

## Adversarial review

Verdict: CLEAN for the local patch, with remote validation pending. Checked reference-counted cleanup, duplicate release, active-host capacity, TTL/eviction bypass, redirect validation and preserved egress-proxy hostname restrictions. Hashing and scene UUID findings do not guard credentials. Lockfile updates retain Linux native assets and passed clean npm installation. Python advisory exceptions were not expanded.

## Local validation

- Full backend coverage gate: 618 suites and 8,761 tests pass; 2 suites / 9 tests remain skipped. Focused image/guard/podcast regressions pass.
- Backend and frontend production builds pass; frontend typecheck and formatter gates pass. Frontend lint has zero errors and 105 existing warnings.
- Frontend: 1,377 component tests on Node 24.20 and 1,732 unit tests pass; targeted coverage remains 100/100/100. Shared media contract: 7 tests pass.
- YouTube sidecar: 653 tests pass, 4 skip on Python 3.14; no production provider requests or listening history are generated by these tests.
- Both npm dependency audits report zero vulnerabilities. Both affected pip-audit lanes pass, preserving the existing 25 AIO compatibility exceptions. Gitleaks scans the PR range cleanly and still catches the synthetic canary.
